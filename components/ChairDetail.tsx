import { Accessibility, Armchair, Cable, MapPin, Plug, Volume2, X } from 'lucide-react';
import { Chair } from '@/types/chair';
import ChairImage from './ChairImage';

export default function ChairDetail({ chair, onClose }: { chair: Chair; onClose: () => void }) {
  const items = [['좌석 수', `${chair.seatCount}석`, Armchair], ['등받이', chair.hasBackrest ? '있음' : '없음', Accessibility], ['콘센트', chair.hasOutlet ? '있음' : '없음', Plug], ['휠체어 접근', chair.wheelchairAccessible ? '가능' : '어려움', Accessibility], ['조용한 공간', chair.isQuiet ? '예' : '아니오', Volume2]] as const;
  return <aside className="soft-card overflow-hidden" aria-label="의자 상세 정보"><div className="relative"><ChairImage src={chair.image} alt={`${chair.name} 사진`} className="h-48 w-full" /><button onClick={onClose} className="absolute right-3 top-3 grid h-9 w-9 place-items-center rounded-full bg-white/90 text-slate-700 shadow" aria-label="상세 패널 닫기"><X size={19} /></button></div><div className="p-5"><div className="mb-4"><div className="mb-2 flex items-center gap-2 text-xs font-bold text-[#0d9c9a]"><MapPin size={14} />{chair.building} · {chair.floor}</div><h2 className="text-xl font-extrabold">{chair.name}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{chair.location}</p></div><p className="mb-5 border-l-2 border-teal-400 pl-3 text-sm leading-6 text-slate-600">{chair.description}</p><div className="grid grid-cols-2 gap-2">{items.map(([label, value, Icon]) => <div key={label} className="rounded-xl bg-slate-50 p-3"><Icon size={17} className="mb-2 text-[#1668c7]" /><span className="block text-xs text-slate-500">{label}</span><strong className="text-sm">{value}</strong></div>)}</div></div></aside>;
}
